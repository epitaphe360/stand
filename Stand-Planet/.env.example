# ================================
# STAND-PLANET - ENVIRONMENT VARIABLES
# ================================

# --------------------------------------------
# SUPABASE CONFIGURATION (REQUIRED)
# --------------------------------------------
# Get these from: https://app.supabase.com/project/[your-project]/settings/api

# Supabase Project URL
VITE_SUPABASE_URL=https://[your-project-ref].supabase.co

# Supabase Anonymous Key (public, safe for client)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Service Role Key (PRIVATE, server-only)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Database Password
SUPABASE_DB_PASSWORD=your-database-password

# --------------------------------------------
# DATABASE CONFIGURATION
# --------------------------------------------
# Supabase PostgreSQL Connection String
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT].supabase.co:5432/postgres

# Alternative: Supabase Pooler (for serverless, recommended for Railway)
# DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# --------------------------------------------
# SESSION & SECURITY
# --------------------------------------------
# Generate secure random key: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_HEX

# --------------------------------------------
# ENVIRONMENT
# --------------------------------------------
NODE_ENV=production

# Server Port (Railway assigns automatically via PORT env var)
PORT=5000

# --------------------------------------------
# SUPABASE STORAGE (Optional)
# --------------------------------------------
# Storage buckets created:
# - public (for images, accessible without auth)
# - private (for user files, requires auth)
# - avatars (for profile pictures)
# - 3d-models (for 3D model files .glb)

# 3D Models bucket name
VITE_SUPABASE_3D_MODELS_BUCKET=3d-models

# --------------------------------------------
# EPITAPHEV1 INTEGRATION (Optional)
# --------------------------------------------
# Enable integration with parent epitaphev1 project
VITE_INTEGRATED_MODE=true

# Parent project name
VITE_PARENT_PROJECT=epitaphev1

# Shared authentication storage key (for SSO)
# IMPORTANT: Must match epitaphev1 auth storage key for Single Sign-On
VITE_AUTH_STORAGE_KEY=epitaphev1-auth-token

# --------------------------------------------
# DRIZZLE ORM
# --------------------------------------------
# Migrations folder
DRIZZLE_OUT=./migrations

# --------------------------------------------
# RAILWAY SPECIFIC (Auto-injected)
# --------------------------------------------
# These are automatically provided by Railway:
# - RAILWAY_ENVIRONMENT
# - RAILWAY_PROJECT_ID
# - RAILWAY_SERVICE_ID
# - RAILWAY_DEPLOYMENT_ID

# --------------------------------------------
# CORS (Optional - for production)
# --------------------------------------------
# Allowed origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# --------------------------------------------
# NOTES
# --------------------------------------------
# 1. NEVER commit .env to git (already in .gitignore)
# 2. Copy this file to .env and fill with real values
# 3. For local dev: use Supabase local project or free tier
# 4. For production: use Railway + Supabase production
# 5. Supabase Free Tier: 500MB database, 1GB file storage
# 6. Generate SESSION_SECRET with: openssl rand -hex 64
